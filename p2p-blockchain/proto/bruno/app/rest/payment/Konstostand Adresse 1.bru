meta {
  name: Konstostand Adresse 1
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/balance?VSAddress={{address1}}
  body: none
  auth: inherit
}

params:query {
  VSAddress: {{address1}}
}

settings {
  encodeUrl: true
  timeout: 0
}
