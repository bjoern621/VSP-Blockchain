meta {
  name: Verlauf Adresse 1
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/history?VSAddress={{address1}}
  body: none
  auth: inherit
}

params:query {
  VSAddress: {{address1}}
}

settings {
  encodeUrl: true
  timeout: 0
}
